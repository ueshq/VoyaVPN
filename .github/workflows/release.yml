name: Release

on:
  workflow_dispatch:
    inputs:
      channel:
        description: Release channel label used for artifact names and updater metadata.
        required: true
        default: beta
        type: choice
        options:
          - beta
          - stable
      build_profile:
        description: Use debug for credential-free validation; release is for prepared signing environments.
        required: true
        default: debug
        type: choice
        options:
          - debug
          - release
      dry_run:
        description: Keep true for validation runs without real updater signatures or publication.
        required: true
        default: true
        type: boolean
      updater_metadata:
        description: Generate latest.json from signed updater artifacts or dry-run placeholders.
        required: true
        default: true
        type: boolean

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  NODE_VERSION: "22"
  PNPM_VERSION: "11.5.0"
  RUST_TOOLCHAIN_VERSION: "1.96.0"

defaults:
  run:
    shell: bash

jobs:
  preflight:
    name: Release Preflight
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Reject stable dry runs
        if: ${{ inputs.channel == 'stable' && inputs.dry_run == true }}
        run: |
          echo "::error title=Invalid stable dispatch::Stable releases must use signed updater artifacts and cannot run with dry_run=true."
          exit 1

      - name: Reject stable debug builds
        if: ${{ inputs.channel == 'stable' && inputs.build_profile != 'release' }}
        run: |
          echo "::error title=Invalid stable dispatch::Stable releases must use build_profile=release."
          exit 1

      - name: Reject stable without updater metadata
        if: ${{ inputs.channel == 'stable' && inputs.updater_metadata != true }}
        run: |
          echo "::error title=Invalid stable dispatch::Stable releases must generate updater metadata evidence."
          exit 1

      - name: Validate non-dry-run updater signing secret
        if: ${{ inputs.dry_run == false }}
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PATH: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PATH }}
        run: |
          if [ -z "${TAURI_SIGNING_PRIVATE_KEY:-}" ] && [ -z "${TAURI_SIGNING_PRIVATE_KEY_PATH:-}" ]; then
            echo "::error title=Missing secret::TAURI_SIGNING_PRIVATE_KEY or TAURI_SIGNING_PRIVATE_KEY_PATH is required when dry_run is false."
            exit 1
          fi

      - name: Validate stable release prerequisites
        if: ${{ inputs.channel == 'stable' && inputs.dry_run == false }}
        env:
          VOYAVPN_CDN_BASE_URL: ${{ vars.VOYAVPN_CDN_BASE_URL }}
          VOYAVPN_UPDATES_BASE_URL: ${{ vars.VOYAVPN_UPDATES_BASE_URL }}
          VOYAVPN_UPDATER_PUBLIC_KEY: ${{ vars.VOYAVPN_UPDATER_PUBLIC_KEY }}
          VOYAVPN_DIAGNOSTICS_ENDPOINT: ${{ vars.VOYAVPN_DIAGNOSTICS_ENDPOINT }}
          VOYAVPN_CORE_ASSETS_JSON: ${{ vars.VOYAVPN_CORE_ASSETS_JSON }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PATH: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PATH }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          WINDOWS_CERTIFICATE_BASE64: ${{ secrets.WINDOWS_CERTIFICATE_BASE64 }}
          WINDOWS_CERTIFICATE_PASSWORD: ${{ secrets.WINDOWS_CERTIFICATE_PASSWORD }}
        run: |
          node <<'NODE'
          const placeholder = /placeholder|replace_before_release|replace-before-release|changeme|\btodo\b|\btbd\b|voyavpn\.example/i;

          function requireValue(name) {
            const value = process.env[name]?.trim() ?? "";
            if (!value) {
              throw new Error(`${name} is required for stable releases.`);
            }
            if (placeholder.test(value)) {
              throw new Error(`${name} must not be a placeholder.`);
            }
            return value;
          }

          function requireOneOf(...names) {
            const present = names.some((name) => (process.env[name]?.trim() ?? "").length > 0);
            if (!present) {
              throw new Error(`${names.join(" or ")} is required for stable releases.`);
            }
            for (const name of names) {
              const value = process.env[name]?.trim() ?? "";
              if (value && placeholder.test(value)) {
                throw new Error(`${name} must not be a placeholder.`);
              }
            }
          }

          function forbiddenHost(hostname) {
            const host = hostname.toLowerCase();
            return (
              host === "example.com" ||
              host.endsWith(".example.com") ||
              host.endsWith(".example") ||
              host.includes("example") ||
              host === "github.com" ||
              host.endsWith(".github.com") ||
              host === "githubusercontent.com" ||
              host.endsWith(".githubusercontent.com") ||
              host === "github.io" ||
              host.endsWith(".github.io") ||
              host === "localhost" ||
              host === "127.0.0.1" ||
              host === "::1" ||
              host.endsWith(".test") ||
              host.includes("placeholder") ||
              host.includes(":") ||
              /^\d{1,3}(?:\.\d{1,3}){3}$/.test(host)
            );
          }

          function requireHttpsUrl(name, { diagnostics = false } = {}) {
            const value = requireValue(name);
            let parsed;
            try {
              parsed = new URL(value);
            } catch {
              throw new Error(`${name} must be a valid URL.`);
            }
            if (parsed.protocol !== "https:") {
              throw new Error(`${name} must use https.`);
            }
            if (forbiddenHost(parsed.hostname)) {
              throw new Error(`${name} must use an approved non-placeholder CDN or diagnostics host.`);
            }
            if (diagnostics && (parsed.username || parsed.password || parsed.search || parsed.hash)) {
              throw new Error(`${name} must not include credentials, query strings, or fragments.`);
            }
          }

          requireHttpsUrl("VOYAVPN_CDN_BASE_URL");
          requireHttpsUrl("VOYAVPN_UPDATES_BASE_URL");
          requireHttpsUrl("VOYAVPN_DIAGNOSTICS_ENDPOINT", { diagnostics: true });
          const publicKey = requireValue("VOYAVPN_UPDATER_PUBLIC_KEY");
          if (publicKey.length < 32) {
            throw new Error("VOYAVPN_UPDATER_PUBLIC_KEY must be the approved Tauri updater public key.");
          }
          const coreAssets = JSON.parse(requireValue("VOYAVPN_CORE_ASSETS_JSON"));
          if (!Array.isArray(coreAssets.assets) || coreAssets.assets.length === 0) {
            throw new Error("VOYAVPN_CORE_ASSETS_JSON must contain a non-empty assets array.");
          }
          requireOneOf("TAURI_SIGNING_PRIVATE_KEY", "TAURI_SIGNING_PRIVATE_KEY_PATH");
          [
            "APPLE_CERTIFICATE",
            "APPLE_CERTIFICATE_PASSWORD",
            "APPLE_ID",
            "APPLE_PASSWORD",
            "APPLE_TEAM_ID",
            "WINDOWS_CERTIFICATE_BASE64",
            "WINDOWS_CERTIFICATE_PASSWORD",
          ].forEach(requireValue);
          NODE

          pnpm tauri:stable-updater-config

  package:
    name: Package ${{ matrix.release_target }}
    needs: preflight
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-15-intel
            release_target: darwin-x86_64
            rust_target: x86_64-apple-darwin
          - os: macos-15
            release_target: darwin-aarch64
            rust_target: aarch64-apple-darwin
          - os: windows-2025
            release_target: windows-x86_64
            rust_target: x86_64-pc-windows-msvc
          - os: windows-11-arm
            release_target: windows-aarch64
            rust_target: aarch64-pc-windows-msvc
          - os: ubuntu-24.04
            release_target: linux-x86_64
            rust_target: x86_64-unknown-linux-gnu
          - os: ubuntu-24.04-arm
            release_target: linux-aarch64
            rust_target: aarch64-unknown-linux-gnu
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Install Linux Tauri dependencies
        if: ${{ runner.os == 'Linux' }}
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential \
            curl \
            file \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libssl-dev \
            libwebkit2gtk-4.1-dev \
            libxdo-dev \
            wget

      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
        with:
          toolchain: ${{ env.RUST_TOOLCHAIN_VERSION }}

      - name: Install Rust target
        run: rustup target add ${{ matrix.rust_target }}

      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Tauri package
        env:
          CI: "true"
          CHANNEL: ${{ inputs.channel }}
          DRY_RUN: ${{ inputs.dry_run }}
          VOYAVPN_RELEASE_CHANNEL: ${{ inputs.channel }}
          VOYAVPN_UPDATES_BASE_URL: ${{ vars.VOYAVPN_UPDATES_BASE_URL }}
          VOYAVPN_UPDATER_PUBLIC_KEY: ${{ vars.VOYAVPN_UPDATER_PUBLIC_KEY }}
          RELEASE_PROFILE: ${{ inputs.build_profile }}
          TAURI_TARGET: ${{ matrix.rust_target }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PATH: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PATH }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          WINDOWS_CERTIFICATE_BASE64: ${{ secrets.WINDOWS_CERTIFICATE_BASE64 }}
          WINDOWS_CERTIFICATE_PASSWORD: ${{ secrets.WINDOWS_CERTIFICATE_PASSWORD }}
        run: |
          build_args=(--target "$TAURI_TARGET")
          if [ "$RELEASE_PROFILE" = "debug" ]; then
            build_args+=(--debug)
          fi

          pnpm tauri:build "${build_args[@]}"

          bundle_dir="target/${TAURI_TARGET}/${RELEASE_PROFILE}/bundle"
          if [ ! -d "$bundle_dir" ]; then
            bundle_dir="target/${RELEASE_PROFILE}/bundle"
          fi
          if [ ! -d "$bundle_dir" ]; then
            echo "::error title=Missing bundle directory::No Tauri bundle directory found for $TAURI_TARGET and profile $RELEASE_PROFILE."
            exit 1
          fi
          echo "BUNDLE_DIR=$bundle_dir" >> "$GITHUB_ENV"

      - name: Normalize artifacts and write checksums
        run: |
          node scripts/release-artifacts.mjs \
            --input "$BUNDLE_DIR" \
            --output "dist/release/${{ matrix.release_target }}" \
            --target "${{ matrix.release_target }}" \
            --channel "${{ inputs.channel }}"

      - name: Upload package artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: voyavpn-${{ inputs.channel }}-${{ matrix.release_target }}-${{ inputs.build_profile }}
          path: dist/release/${{ matrix.release_target }}
          if-no-files-found: error

  cdn-staging-metadata:
    name: CDN Staging Metadata
    needs: package
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Download package artifacts
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          path: dist/downloaded-release-artifacts

      - name: Generate CDN release index
        env:
          CHANNEL: ${{ inputs.channel }}
          VOYAVPN_CDN_BASE_URL: ${{ vars.VOYAVPN_CDN_BASE_URL }}
        run: |
          if [ "$CHANNEL" = "stable" ] && [ -z "${VOYAVPN_CDN_BASE_URL:-}" ]; then
            echo "::error title=Missing variable::VOYAVPN_CDN_BASE_URL is required for stable CDN staging metadata."
            exit 1
          fi

          base_url="${VOYAVPN_CDN_BASE_URL:-https://cdn.voyavpn.test/${CHANNEL}}"
          node scripts/release-index.mjs \
            --input dist/downloaded-release-artifacts \
            --out dist/cdn-staging/release-index.json \
            --channel "$CHANNEL" \
            --base-url "$base_url"

      - name: Upload CDN staging metadata
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: voyavpn-${{ inputs.channel }}-cdn-staging-metadata-${{ inputs.build_profile }}
          path: dist/cdn-staging
          if-no-files-found: error

  updater-metadata:
    name: Updater Metadata
    if: ${{ inputs.updater_metadata == true }}
    needs: package
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Download package artifacts
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          path: dist/downloaded-release-artifacts

      - name: Generate latest.json
        env:
          CHANNEL: ${{ inputs.channel }}
          DRY_RUN: ${{ inputs.dry_run }}
          RELEASE_VERSION: ${{ github.ref_name }}
          VOYAVPN_UPDATES_BASE_URL: ${{ vars.VOYAVPN_UPDATES_BASE_URL }}
        run: |
          if [ "$CHANNEL" = "stable" ] && [ "$DRY_RUN" = "true" ]; then
            echo "::error title=Invalid stable metadata::Stable updater metadata cannot use dry-run signatures."
            exit 1
          fi
          if [ "$CHANNEL" = "stable" ] && [ -z "${VOYAVPN_UPDATES_BASE_URL:-}" ]; then
            echo "::error title=Missing variable::VOYAVPN_UPDATES_BASE_URL is required for stable updater metadata."
            exit 1
          fi

          base_url="${VOYAVPN_UPDATES_BASE_URL:-https://cdn.voyavpn.test/${CHANNEL}/updater}"
          placeholder_flag=""
          if [ "$DRY_RUN" = "true" ]; then
            placeholder_flag="--placeholder-signatures"
          fi

          node scripts/release-updater-metadata.mjs \
            --input dist/downloaded-release-artifacts \
            --out dist/updater/latest.json \
            --channel "$CHANNEL" \
            --base-url "$base_url" \
            $placeholder_flag

      - name: Upload updater metadata
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: voyavpn-${{ inputs.channel }}-updater-metadata-${{ inputs.build_profile }}
          path: dist/updater
          if-no-files-found: error

  core-staging-metadata:
    name: Core Staging Metadata
    needs: package
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Generate core asset metadata
        env:
          CHANNEL: ${{ inputs.channel }}
          VOYAVPN_CDN_BASE_URL: ${{ vars.VOYAVPN_CDN_BASE_URL }}
          VOYAVPN_CORE_ASSETS_JSON: ${{ vars.VOYAVPN_CORE_ASSETS_JSON }}
        run: |
          mkdir -p dist/core-staging

          if [ "$CHANNEL" = "stable" ]; then
            if [ -z "${VOYAVPN_CDN_BASE_URL:-}" ]; then
              echo "::error title=Missing variable::VOYAVPN_CDN_BASE_URL is required for stable core staging metadata."
              exit 1
            fi
            if [ -z "${VOYAVPN_CORE_ASSETS_JSON:-}" ]; then
              echo "::error title=Missing variable::VOYAVPN_CORE_ASSETS_JSON is required for stable core staging metadata."
              exit 1
            fi
            printf '%s\n' "$VOYAVPN_CORE_ASSETS_JSON" > dist/core-staging/source-core-assets.json
          else
            cp tests/fixtures/release/core-assets.json dist/core-staging/source-core-assets.json
          fi

          base_url="${VOYAVPN_CDN_BASE_URL:-https://cdn.voyavpn.test/${CHANNEL}}"
          node scripts/core-assets.mjs \
            --fixture dist/core-staging/source-core-assets.json \
            --out dist/core-staging/core-assets.json \
            --channel "$CHANNEL" \
            --base-url "$base_url"

      - name: Upload core staging metadata
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: voyavpn-${{ inputs.channel }}-core-staging-metadata-${{ inputs.build_profile }}
          path: dist/core-staging
          if-no-files-found: error

  final-stable-readiness:
    name: Final Stable Readiness
    if: ${{ inputs.channel == 'stable' && inputs.dry_run == false }}
    needs:
      - package
      - cdn-staging-metadata
      - updater-metadata
      - core-staging-metadata
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Download darwin x64 package artifact
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: voyavpn-${{ inputs.channel }}-darwin-x86_64-${{ inputs.build_profile }}
          path: dist/final-readiness/packages/darwin-x86_64

      - name: Download darwin arm64 package artifact
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: voyavpn-${{ inputs.channel }}-darwin-aarch64-${{ inputs.build_profile }}
          path: dist/final-readiness/packages/darwin-aarch64

      - name: Download Windows x64 package artifact
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: voyavpn-${{ inputs.channel }}-windows-x86_64-${{ inputs.build_profile }}
          path: dist/final-readiness/packages/windows-x86_64

      - name: Download Windows arm64 package artifact
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: voyavpn-${{ inputs.channel }}-windows-aarch64-${{ inputs.build_profile }}
          path: dist/final-readiness/packages/windows-aarch64

      - name: Download Linux x64 package artifact
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: voyavpn-${{ inputs.channel }}-linux-x86_64-${{ inputs.build_profile }}
          path: dist/final-readiness/packages/linux-x86_64

      - name: Download Linux arm64 package artifact
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: voyavpn-${{ inputs.channel }}-linux-aarch64-${{ inputs.build_profile }}
          path: dist/final-readiness/packages/linux-aarch64

      - name: Download CDN staging metadata
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: voyavpn-${{ inputs.channel }}-cdn-staging-metadata-${{ inputs.build_profile }}
          path: dist/final-readiness/cdn-staging-metadata

      - name: Download updater metadata
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: voyavpn-${{ inputs.channel }}-updater-metadata-${{ inputs.build_profile }}
          path: dist/final-readiness/updater-metadata

      - name: Download core staging metadata
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: voyavpn-${{ inputs.channel }}-core-staging-metadata-${{ inputs.build_profile }}
          path: dist/final-readiness/core-staging-metadata

      - name: Validate final stable readiness
        env:
          VOYAVPN_CDN_BASE_URL: ${{ vars.VOYAVPN_CDN_BASE_URL }}
          VOYAVPN_UPDATES_BASE_URL: ${{ vars.VOYAVPN_UPDATES_BASE_URL }}
          VOYAVPN_UPDATER_PUBLIC_KEY: ${{ vars.VOYAVPN_UPDATER_PUBLIC_KEY }}
          VOYAVPN_DIAGNOSTICS_ENDPOINT: ${{ vars.VOYAVPN_DIAGNOSTICS_ENDPOINT }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PATH: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PATH }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          WINDOWS_CERTIFICATE_BASE64: ${{ secrets.WINDOWS_CERTIFICATE_BASE64 }}
          WINDOWS_CERTIFICATE_PASSWORD: ${{ secrets.WINDOWS_CERTIFICATE_PASSWORD }}
        run: |
          node scripts/check-release-readiness.mjs \
            --mode stable \
            --cdn-base-url "$VOYAVPN_CDN_BASE_URL" \
            --updates-base-url "$VOYAVPN_UPDATES_BASE_URL" \
            --diagnostics-endpoint "$VOYAVPN_DIAGNOSTICS_ENDPOINT" \
            --release-artifacts dist/final-readiness/packages \
            --updater-artifacts dist/final-readiness/packages \
            --core-assets dist/final-readiness/core-staging-metadata/source-core-assets.json \
            --release-index dist/final-readiness/cdn-staging-metadata/release-index.json \
            --updater-metadata dist/final-readiness/updater-metadata/latest.json \
            --core-manifest dist/final-readiness/core-staging-metadata/core-assets.json \
            --tauri-config dist/final-readiness/packages/darwin-x86_64/stable-updater-config.json \
            --work-dir dist/final-readiness/readiness-output

      - name: Upload final readiness evidence
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: voyavpn-${{ inputs.channel }}-final-readiness-${{ inputs.build_profile }}
          path: dist/final-readiness/readiness-output
          if-no-files-found: error
