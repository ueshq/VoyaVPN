interface:
  display_name: "Claude Plan Rollout"
  short_description: "Plan large refactors and migrations driven by Claude Code"
  default_prompt: "Use $claude-plan-rollout to draft a spec, a phase-and-batch plan, and, when appropriate, a rollout runner that drives `claude -p` for this initiative."
